var config = {
  // Prefix for env variable overrides
  prefix: 'myapp',
  // Service settings
  service: {
    port: 8080
  },
  foo: {
    too: 'much',
    bar: {
      baz: 'quz'
    },
    a: 'b'
  },
  c: 'd'
};

module.exports = config;